/* ============================================================
   Map: map import/remove/scale controls (master only)
   ============================================================ */

(function() {
  'use strict';

  // Will be initialized in main.js
  window.RPG = window.RPG || {};
  window.RPG.map = window.RPG.map || {};
})();
